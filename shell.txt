1、shell外壳：包裹在操作系统外层的软件层，方便用户和OS进行沟通
shell：接受用户的输入，交给OS执行，得到结果反馈给用户

2、shell作用：a.交互  b.保护操作系统

3、shell vs bash：shell是所有外壳的统称，bash是shell中的某一种



Linux内核（kernel）：
1、广义上的操作系统：shell+kernel+配套的软件（应用+驱动等）
2、狭义上的操作系统：指的就是内核，纯的操作系统